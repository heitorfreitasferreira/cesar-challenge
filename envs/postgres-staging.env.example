# POSTGRES staging — exemplo (commitado). Selar para sealed-postgres.yaml:
#   kubectl create secret generic postgres-credentials -n todolist-staging \
#     --from-env-file=envs/postgres-staging.env --dry-run=client -o yaml | \
#   kubeseal --cert clusters/desafio/sealed-secrets/cert.pem --scope strict \
#     --namespace todolist-staging --name postgres-credentials --format yaml \
#   > k8s/overlays/staging/sealed-postgres.yaml
POSTGRES_USER=todolist
POSTGRES_PASSWORD=
POSTGRES_DB=todolist
