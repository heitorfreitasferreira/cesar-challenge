# POSTGRES production — exemplo (commitado). Selar para sealed-postgres.yaml:
#   kubectl create secret generic postgres-credentials -n todolist-production \
#     --from-env-file=envs/production.env --dry-run=client -o yaml | \
#   kubeseal --cert clusters/desafio/sealed-secrets/cert.pem --scope strict \
#     --namespace todolist-production --name postgres-credentials --format yaml \
#   > k8s/overlays/production/sealed-postgres.yaml
POSTGRES_USER=todolist
POSTGRES_PASSWORD=
POSTGRES_DB=todolist
