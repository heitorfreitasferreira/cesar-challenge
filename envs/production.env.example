# PRODUCTION — exemplo tangivel (commitado). Copie para production.env (gitignored),
# preencha valores SINTETICOS e sele com:
#   kubectl create secret generic todolist-auth -n todolist-production \
#     --from-env-file=envs/production.env --dry-run=client -o yaml | \
#   kubeseal --cert clusters/desafio/sealed-secrets/cert.pem --scope strict \
#     --namespace todolist-production --name todolist-auth --format yaml \
#   > k8s/overlays/production/sealed-auth.yaml
# Mesmo fluxo para postgres (chaves POSTGRES_* -> sealed-postgres.yaml).
# NUNCA commitar o .env preenchido: so .env.example + SealedSecret cifrado.
ADMIN_USER=
ADMIN_PASSWORD=
SESSION_KEY=
CLEANUP_TOKEN=
