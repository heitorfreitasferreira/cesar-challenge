apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace-rbac.yaml
  - postgres.yaml
  - app.yaml
  - ingress.yaml
  - cleanup-cronjob.yaml
# todolist-config vem de configMapGenerator em cada overlay (nome com hash),
# para mudanca de conteudo disparar rollout. Ver DECISIONS.
